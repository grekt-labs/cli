import { describe, test } from "bun:test";

describe("scanner", () => {
  test.todo("scanArtifact finds agent.md");
  test.todo("scanArtifact finds skills in skills/");
  test.todo("scanArtifact finds commands in commands/");
  test.todo("scanArtifact validates frontmatter");
  test.todo("scanArtifact returns empty arrays when no components");
});
