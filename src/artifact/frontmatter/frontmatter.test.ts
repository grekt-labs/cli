import { describe, test } from "bun:test";

describe("frontmatter", () => {
  test.todo("parseFrontmatter extracts YAML from markdown");
  test.todo("parseFrontmatter validates against schema");
  test.todo("parseFrontmatter returns null for invalid frontmatter");
  test.todo("parseFrontmatter handles missing frontmatter");
});
