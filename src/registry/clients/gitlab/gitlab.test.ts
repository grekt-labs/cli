import { describe, test } from "bun:test";

describe("gitlab client", () => {
  test.todo("getVersions lists package versions");
  test.todo("getInfo returns package metadata");
  test.todo("download fetches from generic package registry");
  test.todo("publish uploads to generic package registry");
  test.todo("deprecate updates package metadata");
  test.todo("undeprecate removes deprecation from metadata");
});
