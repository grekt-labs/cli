import { describe, test } from "bun:test";

describe("manager", () => {
  test.todo("registerPlugin adds plugin to registry");
  test.todo("getPlugin returns registered plugin");
  test.todo("getPlugin returns undefined for unknown plugin");
  test.todo("listPlugins returns all registered plugins");
  test.todo("validateTarget checks if plugin exists");
});
