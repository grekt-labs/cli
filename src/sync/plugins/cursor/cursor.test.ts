import { describe, test } from "bun:test";

describe("cursor plugin", () => {
  test.todo("syncs to .cursorrules file");
  test.todo("uses markers for managed sections");
  test.todo("preserves manual content outside markers");
});
